VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
# Optional: edge function base (extract / judgment / ingest)
VITE_FUNCTIONS_URL=http://127.0.0.1:54321/functions/v1

# No VITE_ZOHO_* or NEXT_PUBLIC_ZOHO_* — Zoho client id/secret/refresh
# token and ANTHROPIC_API_KEY live in Supabase secrets, set per
# environment with `npx supabase secrets set`, never committed to git.
# The browser only talks to our Edge Functions.
